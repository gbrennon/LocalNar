[package]
name = "localnar-application"
description = "Application services for LocalNar: inbound and outbound ports plus the services that install, search, verify, inspect, remove, list, and prune models."
edition.workspace = true
version.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
localnar-domain.workspace = true

[[test]]
name = "services"
path = "tests/services/mod.rs"
